import type { MutationToolDefinition } from "./context.js";
import {
  CREATE_NODE_CONTRACT,
  CREATE_VOLUME_CONTRACT,
  DELETE_NODE_CONTRACT,
  DELETE_VOLUME_CONTRACT,
  RENAME_NODE_CONTRACT,
  REPARENT_NODE_CONTRACT,
  SET_NODE_COMPONENTS_CONTRACT,
  SET_NODE_METADATA_CONTRACT,
  SET_NODE_TRANSFORM_CONTRACT,
  createNode,
  createVolume,
  deleteNode,
  deleteVolume,
  renameNode,
  reparentNode,
  setNodeComponents,
  setNodeMetadata,
  setNodeTransform,
} from "./scene.js";
import {
  CREATE_MATERIAL_CONTRACT,
  DELETE_MATERIAL_CONTRACT,
  UPDATE_MATERIAL_CONTRACT,
  createMaterial,
  deleteMaterial,
  updateMaterial,
} from "./materials.js";
import {
  COPY_REGION_CONTRACT,
  DELETE_REGION_CONTRACT,
  FILL_BOX_CONTRACT,
  FILL_CYLINDER_CONTRACT,
  FILL_SPHERE_CONTRACT,
  MIRROR_REGION_CONTRACT,
  REMOVE_VOXEL_BATCH_CONTRACT,
  REPLACE_VOXEL_MATERIAL_CONTRACT,
  ROTATE_REGION_CONTRACT,
  SET_VOXEL_BATCH_CONTRACT,
  TRANSLATE_REGION_CONTRACT,
  copyRegion,
  deleteRegion,
  fillBox,
  fillCylinder,
  fillSphere,
  mirrorRegion,
  removeVoxelBatch,
  replaceVoxelMaterial,
  rotateRegion,
  setVoxelBatch,
  translateRegion,
} from "./geometry.js";

/**
 * Every v1 mutation tool in stable registry order (plan S11.5/S11.6,
 * ticket #32): scene, material, and coarse-geometry command constructors.
 * This list is the single source for the registry, the mutator dispatch,
 * and the public exports, so adding a tool touches exactly one file.
 */
export const MUTATION_TOOL_DEFINITIONS: readonly MutationToolDefinition[] =
  Object.freeze([
    { contract: CREATE_NODE_CONTRACT, handler: createNode },
    { contract: DELETE_NODE_CONTRACT, handler: deleteNode },
    { contract: RENAME_NODE_CONTRACT, handler: renameNode },
    { contract: REPARENT_NODE_CONTRACT, handler: reparentNode },
    { contract: SET_NODE_TRANSFORM_CONTRACT, handler: setNodeTransform },
    { contract: SET_NODE_COMPONENTS_CONTRACT, handler: setNodeComponents },
    { contract: SET_NODE_METADATA_CONTRACT, handler: setNodeMetadata },
    { contract: CREATE_VOLUME_CONTRACT, handler: createVolume },
    { contract: DELETE_VOLUME_CONTRACT, handler: deleteVolume },
    { contract: CREATE_MATERIAL_CONTRACT, handler: createMaterial },
    { contract: UPDATE_MATERIAL_CONTRACT, handler: updateMaterial },
    { contract: DELETE_MATERIAL_CONTRACT, handler: deleteMaterial },
    { contract: FILL_BOX_CONTRACT, handler: fillBox },
    { contract: FILL_SPHERE_CONTRACT, handler: fillSphere },
    { contract: FILL_CYLINDER_CONTRACT, handler: fillCylinder },
    { contract: SET_VOXEL_BATCH_CONTRACT, handler: setVoxelBatch },
    { contract: REMOVE_VOXEL_BATCH_CONTRACT, handler: removeVoxelBatch },
    {
      contract: REPLACE_VOXEL_MATERIAL_CONTRACT,
      handler: replaceVoxelMaterial,
    },
    { contract: COPY_REGION_CONTRACT, handler: copyRegion },
    { contract: DELETE_REGION_CONTRACT, handler: deleteRegion },
    { contract: TRANSLATE_REGION_CONTRACT, handler: translateRegion },
    { contract: ROTATE_REGION_CONTRACT, handler: rotateRegion },
    { contract: MIRROR_REGION_CONTRACT, handler: mirrorRegion },
  ]);
