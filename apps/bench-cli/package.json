{
  "name": "@voxel-maker/bench-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "voxel-maker-bench": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node ../../scripts/clean.mjs dist",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@voxel-maker/benchmarks": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
