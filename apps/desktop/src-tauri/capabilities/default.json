{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum native allowlist for the desktop shell: core window/event behavior only. Dialogs run in Rust and mint scoped handles for the shell's own project/image commands (issue #94); the webview holds no dialog or filesystem permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
