{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum native allowlist for the desktop shell: core window/event behavior, the open/save dialogs, and the shell's own scoped project I/O commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save"]
}
